{
  "identifier": "default",
  "description": "Default capabilities for Sandbox",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:default",
    "shell:default"
  ]
}
